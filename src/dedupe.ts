import type { Findings } from "./types.js";

/**
 * Placeholder for finding de-duplication logic.
 * Currently returns the findings unchanged until the implementation is added.
 */
export function dedupeFindings(findings: Findings): Findings {
  return findings;
}
