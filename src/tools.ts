/**
 * Placeholder registry for SDK tool bindings.
 * Concrete tool implementations will be added in subsequent iterations.
 */
export type ToolRegistry = Record<string, unknown>;

export const tools: ToolRegistry = {};
